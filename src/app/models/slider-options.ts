export interface SliderOptions {
  slidesPerView: number;
  spaceBetween?: number;
  speed?: number;
  initialSlide?: number;
  centeredSlides?: boolean;
  freeMode?: boolean;
  pager?: boolean;
  paginationType?: string;
  loop?: boolean;
  autoplay?: true;
}
